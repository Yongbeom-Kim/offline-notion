import { Box, Typography } from "@mui/joy";

export const SettingsPageHeader = () => (
	<Box sx={{ backgroundColor: "background.level1" }}>
		<Typography level="h1" sx={{ paddingY: 4 }}>
			Settings
		</Typography>
	</Box>
);
